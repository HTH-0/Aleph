=================
ubuntu 192.168.16.80
=================

sudo ip link set enp0s3 promisc on
sudo ip link set enp0s8 promisc on

sudo ip link set enp0s3 up
sudo ip link set enp0s8 up

sudo apt update -y
sudo apt upgrade -y

sudo apt install -y vim iputils-ping

sudo vi /etc/snort/snort.conf
=========================
65
ipvar HOME_NET 192.168.16.0/24
68
ipvar EXTERNAL_NET !$HOME_NET
758 추가
config daq: afpacket
config daq_mode : inline
config logdir: /var/log/snort
========================

sudo vi /etc/snort/rules/local.rules
======
drop icmp 192.168.16.86 any -> $HOME_NET any (msg:"ICMP Drop Test"; sid:1000001; rev:1;)
======

sudo /usr/sbin/snort -T -c /etc/snort/snort.conf -i enp0s3:enp0s8
sudo snort -A console -Q -c /etc/snort/snort.conf -i enp0s3:enp0s8
